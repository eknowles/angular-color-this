language: node_js
node_js:
  - 0.12
branches:
  only:
    - master
before_script:
  - npm install -g bower
  - npm install phantomjs
  - npm install -g karma
  - npm install -g karma-cli
  - bower install
